# 每日新聞雷達 News Radar — 開發紀錄

## 專案資訊

| 項目 | 內容 |
|------|------|
| 專案名稱 | 每日新聞雷達 News Radar |
| GitHub | https://github.com/carolkao-fin/news-radar |
| 網站 | https://news-radar-tw.streamlit.app |
| 部署平台 | Streamlit Community Cloud + GitHub Actions |
| 開發工具 | Claude Code (claude-opus-5) |
| 技術架構 | Python 3.12 · Streamlit · feedparser · Groq API |
| 開發日期 | 2026-08-01 |

---

## 需求

每天自動彙整三大主題的新聞，分成不同頁面呈現，每則附摘要與資料來源連結。

**明確限制：不採用維基百科，能用官方資料最好。**
另外要讓使用者可以自己新增主題，系統自動產生搜尋設定。

---

## 功能說明

- 三個內建主題：**AI 人工智慧**、**台灣國際貿易與關稅**、**國際重大新聞**
- 每個主題一個獨立頁面，側邊欄依**類別**分組（科技／經貿／國際／自訂⋯）
- 每則新聞顯示：標題連結、官方／媒體標籤、來源名稱、發布時間、中文摘要、重點條列、原始連結
- 使用者可自行新增／編輯／刪除主題，系統自動展開關鍵字並挑選來源
- 每日由 GitHub Actions 於台灣時間 07:00 自動更新並 commit 回 repo
- 資料來源頁可依性質瀏覽所有來源，並實際連線檢查 feed 是否失效
- **下載紀錄頁可自由選擇期間，把過去累積的每日新聞匯出成 Word（.docx）**

---

## 開發歷程

### 階段一 — 來源盤點（最花時間的部分）

需求說「能用官方資料最好」，所以第一步是找出各機關真正還在運作的 RSS。
**結論是不能靠猜網址** —— 政府網站的 feed 路徑幾乎沒有規律，而且失效時不會有任何通知。

作法：抓取各官網首頁 HTML，掃描 `<link rel="alternate" type="...xml">` 與含 `rss`／`feed`
字樣的 `<a href>`，再逐一實際解析確認拿得到 entries。

**台灣政府機關實測結果**

| 機關 | 結果 | 可用的 feed |
|------|------|------------|
| 財政部關務署 | ✅ | `web.customs.gov.tw/Rss/2222`（新聞）、`/Rss/698`（法規預告） |
| 經濟部國際貿易署 | ✅ | `trade.gov.tw/RSS/List.aspx?nodeID=40`（新聞）、`nodeID=45`（國際經貿動態） |
| 財政部 | ✅ | `mof.gov.tw/Rss/384fb3077bb349ea973e7fc6f13b6974` |
| 中央銀行 | ✅ | `cbc.gov.tw/tw/rss-302-1.xml` |
| 經濟部 MOEA | ❌ | 首頁有標示 feed，但實際請求回 503 |
| 國科會 NSTC | ❌ | 回傳 HTML 而非 XML |
| 數位發展部 moda | ❌ | 找不到任何 feed |
| 外交部 MOFA | ❌ | 回傳 HTML |
| 行政院 EY | ❌ | 找不到任何 feed |

國際組織與國外機關則幾乎都正常：WTO、USTR、歐盟執委會（數位政策／貿易總署／新聞室）、
聯合國、NIST、OECD.AI、美國聯邦公報、arXiv。
IMF、World Bank、美國 CBP、Anthropic 的 RSS 都已失效或需要特殊處理，捨棄。

> **教訓**：`web.customs.gov.tw/Rss` 這類路徑其實是一個 HTML 索引頁，
> 真正的 feed 在 `/Rss/2222` 這種子路徑。財政部與國貿署也是同樣結構，
> 必須先抓索引頁再解析出子連結。

---

### 階段二 — 抓取與篩選管線

**流程**：讀 RSS → 時間窗過濾 → 關鍵字評分 → 去重 → 來源分散 → 排序

**關鍵字評分**：標題命中 2 分、內文命中 1 分。

**收錄門檻依來源性質區分**

| 來源性質 | 門檻 |
|---------|------|
| 聚焦型（WTO、關務署、BBC 國際版⋯） | 最近期項目全數收錄 |
| 龐雜型（arXiv、聯邦公報、綜合財經媒體） | 需 ≥ 2 分 |
| Google 新聞搜尋結果 | 查詢本身已是過濾，≥ 1 分即可 |
| 使用者自訂主題借用的來源 | 一律要命中關鍵字（`require_keywords`） |

**去重**：以正規化後的網址與標題比對。標題正規化時會移除尾端的「- 媒體名」。

---

### 階段三 — 實測後修掉的問題

實際跑起來才浮現的問題，每一個都是看了輸出才發現的：

| 問題 | 原因 | 解法 |
|------|------|------|
| arXiv 與美國聯邦公報洗版整頁 | 這兩個 feed 每天數十到數百筆，且都命中關鍵字 | 最終清單中同一來源最多 3 則，額度沒滿才回補 |
| 中央銀行的來源名稱變成一串網址 | 程式對所有 feed 都去解析 `entry.source.title`，但央行那欄放的是網址 | 只有 Google 新聞這類彙整來源才解析真實媒體名 |
| 關務署新聞的摘要只有一條連結 | 該 feed 的 `description` 就是放網址 | 偵測到純網址時視為無摘要，改用標題 |
| 自訂主題抓回 OpenAI 官方公告 | 借用的來源是 `broad=False`，會無條件收錄 | 自訂主題加上 `require_keywords`，所有來源都要命中關鍵字 |
| Google 搜尋回傳四月的舊聞 | Google 新聞是照相關度排序，不是時間 | 查詢自動加上 `when:Nd` 運算子 |
| Nikkei Asia 顯示「時間未提供」 | 該 feed 只給 `dc:date` 字串，沒有標準時間欄位 | 補上字串日期的解析備援 |

---

### 階段四 — 摘要

- 主要：Groq `llama-3.3-70b-versatile`（免費），一次送 6 則，要求回傳 JSON
- 另外產生主題層級的「今日重點」導讀
- **備援**：沒有 `GROQ_API_KEY` 時退回擷取式摘要（清理後的 RSS 描述前段）

刻意**不做全文爬取** —— 多數新聞網站禁止自動抓取內文，且 RSS 的 description
已足以產出可用摘要。所有摘要都保留原始連結供查證。

Prompt 明確要求「只能根據提供的內容撰寫，不可加入未提及的數字、日期或推論；
若原始描述資訊太少，就直接改寫標題並說明資訊有限，不要編造」。

---

### 階段五 — 部署

**GitHub Actions 每日排程**：cron `0 23 * * *`（UTC）= 台灣時間隔天 07:00，
抓取後把 `data/` commit 回 repo，Streamlit Cloud 偵測到新 commit 自動重新部署。

**踩到的雷**

1. **`workflow` scope**：gh CLI 的 token 只有 `repo` scope 時，GitHub 會拒絕推送
   `.github/workflows/` 底下的檔案，錯誤訊息是
   `refusing to allow an OAuth App to create or update workflow ... without workflow scope`。
   解法是先推其他檔案讓網站上線，之後再補上。
2. **`news-radar.streamlit.app` 已被他人佔用**，改用 `news-radar-tw`。
3. **actions/checkout@v4 與 setup-python@v5 會噴 Node 20 淘汰警告**，升到 v7。

排程有手動觸發實測過，確認 59 則資料成功抓取並 commit。

---

### 階段六 — 加入財經媒體

原本以官方來源為主，但官方發布量少、且偏法規公告，缺乏產業動態。
依需求加入財經媒體補足。

**鉅亨網的 RSS 路徑已經改過** —— 網路上流傳的 `/rss/cat/xxx` 和 feedburner 版本全部 404，
真正還在運作的是 `https://news.cnyes.com/rss/v1/news/category/{headline|tw_stock|wd_stock|forex}`，
是掃首頁的 `<link>` 標籤才找到的。

同時加入：經濟日報（產業／國際）、MoneyDJ、科技新報、自由時報國際、中央社、
Nikkei Asia、BBC、德國之聲、公視。
工商時報（Cloudflare 阻擋）、中時、數位時代、天下、商周、INSIDE 的 feed 都已失效。

**加入後暴露的關鍵問題**：貿易頁跑出「麗寶樂園結盟木棉花」「美食展台灣觀光館」
這類完全不相干的報導。原因是關鍵字裡有「**台灣**」，而台灣媒體幾乎每篇報導都會提到台灣，
等於這個關鍵字沒有任何篩選作用。

解法：把「台灣」從貿易主題的關鍵字移除，改由搜尋查詢「台灣 關稅 貿易」負責定位；
國際主題那組空泛的查詢（「國際 情勢 重大」）也整組移除，改靠各家國際版供稿。
修正後官方來源比例從 7 則回升到 13 則。

---

### 階段七 — 主題類別與刪除

**主題類別**：主題數量變多時側邊欄會擠成一長串，因此加上 `category` 欄位，
側邊欄與今日總覽都依類別分組。預設科技／經貿／國際／台灣／自訂，可自訂新類別。

**開放刪除任何主題**：原本內建主題不可刪，只能停用。改為全部可刪，
但因為這有風險，補了三層還原機制：

- 內建主題的出廠定義從 `data/topics.json` 移到 **`core/defaults.py`**，
  JSON 只代表「目前的狀態」
- 主題管理頁偵測到內建主題被刪時，顯示「還原內建主題」按鈕
- 單一主題可「還原出廠設定」；`topics.json` 被清空時開站會自動重建

**資料來源頁重做**：來源加上 `kind` 欄位，依政府機關／國際組織／研究機構／
企業官方／新聞媒體／新聞彙整分組，並顯示每個來源被哪些主題使用。
加上健康檢查按鈕，實際連線抓一次全部來源，找出已失效的 feed —— 
考量到政府網站改版時 feed 會無聲失效，這個功能是必要的。

---

### 階段八 — 收錄天數與自訂來源

**收錄天數從 2 天拉長到 7 天**。原本的窗口太短，官方機關的發布頻率低
（關務署、國貿署常常好幾天才一則），導致官方來源在頁面上幾乎沒有內容。
同時讓每個主題可以自己覆寫天數（主題設定優先於全域值），上限放寬到 60 天。

**使用者自訂 RSS 來源**：新增 `core/source_registry.py`，把內建來源（`sources.py`）
與使用者自訂來源（`data/sources.json`）合併。程式其他地方一律透過 `all_sources()`
取得來源，不再直接引用 `sources.SOURCES`，否則自訂來源不會被看到。

新增來源時會先實際連線驗證，抓不到 entries 就不讓加，並自動判斷語言
（用前 10 則標題的中文字比例）、顯示最新幾則標題讓使用者確認抓對了。
刪除自訂來源時會同時從所有主題的來源清單移除，避免留下無效 id。

**主題可指定「只用指定來源」**：新增 `search_enabled` 開關，關掉之後該主題
完全不使用 Google 新聞搜尋，只從指定的 RSS 來源抓取，適合需要來源可追溯的主題。

**這階段抓到一個潛伏的 bug**：先前為了處理 Nikkei Asia 沒有時間欄位的問題，
寫了 `feedparser._parse_date(raw)` 當備援 —— 但 feedparser 6 已經把這個函式
搬到 `feedparser.datetimes`，頂層根本沒有這個屬性。

之所以一直沒爆，是因為 Nikkei 的 entry 連 `published` 字串欄位都沒有，
那段程式從來沒被執行到。直到加入 ETtoday 財經（日期格式是
`Fri,31 Jul 2026 23:19:00  +0800`，逗號後少一個空格、時區前多一個空格）才觸發。

修正時一併加強：先試 `feedparser.datetimes._parse_date`，失敗就把逗號後補空白、
壓掉多餘空白再試一次，最後退到 `email.utils.parsedate_to_datetime`。

> **教訓**：只在少數 feed 上才會執行到的備援路徑，等於沒有被測試過。
> 加來源的時候順便就會驗證到這些路徑，這也是實際跑資料比單元測試更能發現問題的地方。

---

### 階段九 — 摘要品質

在沒有 Groq 金鑰的情況下，摘要是直接取 RSS 描述前段，但實際看輸出才發現
**各家 feed 的描述夾了大量機器格式**，讀起來像機器吐出來的東西：

| 來源 | 原本顯示的摘要 |
|------|--------------|
| arXiv | `arXiv:2607.28505v1 Announce Type: new Abstract: The rise of...` |
| 歐盟數位政策 | `（重複一次標題）Anonymous (not verified) Fri, 07/31/2026 - 09:00 From…` |
| 歐盟新聞室 | `European Commission Press release Brussels, 31 Jul 2026 The...` |
| 關務署法規預告 | `財政部 公告 發文日期：中華民國115年7月28日 發文字號：台財關字第115...號 附件：如公告事項一 主旨：...` |
| MoneyDJ | `內文連結` |
| Google 新聞 | 標題 + 媒體名 + 一串相關報導標題 |
| Nikkei Asia | 與標題完全相同 |

新增 `clean_summary()` 處理：剝掉樣板前綴與中繼資料、公文表頭只保留「主旨」與「依據」、
去掉開頭重複標題的部分與結尾的 `[…]`。

**關鍵決定**：判定為沒有可用內容時**回傳空字串，而不是硬塞一段文字**。
畫面上會標示「此來源只提供標題，點連結閱讀原文」——
與其顯示「內文連結」這種毫無意義的字，不如誠實說明。
實測 75 則中有 62 則有可用摘要、13 則只有標題。

同時補上兩個讓摘要真的能用起來的東西：

1. **金鑰可以在網站上直接輸入**（存在 session_state，優先於環境變數與 secrets），
   附測試按鈕實際打一次 API 確認有效。沿用 gri-analyzer 的作法，
   不必為了試用去改 secrets。
2. **「重新產生摘要」** —— 只針對已抓下來的新聞重寫摘要，不重新連線抓取。
   設定金鑰後最常用的操作，不需要為了換摘要再跑一次完整抓取。
   命令列對應 `--resummarize`。

---

### 階段十 — 關鍵字自動生成與官方報告

**官方報告來源**：盤點政府與國際組織的報告／統計 feed，結果一樣是實測才知道。

| 來源 | 結果 |
|------|------|
| 美國 GAO 稽核報告、CBO 成本估計、普查局經濟指標、聯準會 | ✅ 可用 |
| 中華經濟研究院（政府智庫） | ✅ 可用，`cier.edu.tw/feed/` |
| 歐洲央行出版品 | ⚠️ 三次有一次 30 秒逾時，內容偏學術，不納入 |
| IMF、OECD、UNCTAD、BLS、USITC、CRS | ❌ 一律回 403（擋自動抓取） |
| WTO 出版品、EUR-Lex、Eurostat | ❌ 網址已失效 |
| 主計總處、國發會、統計資訊網、台經院、資策會 MIC | ❌ **完全沒有 RSS** |

台灣的統計機關到今天仍然沒有任何 feed，這是既有的限制，不是這次才發現的。
新增 `report` 來源類別（📄 官方報告與統計）與這 5 個來源後，
貿易主題實際抓到了美國普查局的「Advance U.S. International Trade in Goods」
與中經院對 301 關稅的分析。

**關鍵字自動生成**：原本沒有金鑰時，「半導體出口管制」只會展開出 3 個關鍵字，
因為中文沒有空白可以斷詞。改成三條路徑：

1. **領域詞庫**（`core/keywords.py`）—— 涵蓋半導體、關稅、出口管制、供應鏈、
   碳邊境、AI、能源、稀土、匯率等 30 多組概念與其同義詞、英文對照。
   同一個例子現在展開出 20 個關鍵字。
2. **♻️ 重新產生** —— 依名稱重新展開並**與現有關鍵字合併**，不會蓋掉手動調整。
3. **💡 從實際新聞建議** —— 試抓一次，統計實際出現的高頻詞。不依賴預先定義，
   冷門主題也有用。

第 3 條的中文處理踩到兩個坑，都是看實際輸出才發現的：

- 中文沒有斷詞，改用 2–4 字 n-gram 統計，結果建議出「導體」「半導」「積電」
  這種**既有關鍵字的碎片**。加上兩道過濾：候選詞若是既有關鍵字的子字串就丟掉
  （反過來若「包含」既有關鍵字則保留，那是更具體的詞）；重疊的候選取較長的那個。
- 高頻詞的前幾名是「台灣」「中國」「科技」這種**幾乎沒有篩選力的詞**。
  這正是階段六踩過的坑，所以標記為 `TOO_BROAD`，仍然列出讓使用者判斷，
  但**不預設勾選**。

修正後同一個主題建議出來的是：供應鏈、記憶體、先進封裝、研發、競爭 —— 都是可用的。

---

### 階段十一 — 歷史紀錄匯出成 Word

原本「歷史資料」只能下載**最新那一天**的 JSON，等於累積下來的快照沒辦法拿來用。
改成獨立的「📥 下載紀錄」頁（`core/report.py` + `streamlit_app.py: download_page`），
使用者自由選擇期間後匯出成 `.docx`。

**期間選擇**：`st.date_input` 的區間模式，上限／下限自動夾在實際有快照的範圍內，
另外提供最近 7／30／90 天與全部四個快速按鈕。按鈕的作法是直接改寫
`st.session_state["dl_range"]` 再 `st.rerun()` —— 因為按鈕排在 `date_input` 前面渲染，
這時候該 widget 這一輪還沒建立，改 session_state 不會觸發 Streamlit 的
「同時給了 default 與 session state」警告。選到一半只有起始日時，
`date_input` 回傳的是 1 元素 tuple，要擋掉不然會 unpack 失敗。

**Word 產出**（python-docx，不需要 Word 或 LibreOffice）：

- 封面：期間、天數、則數、官方來源數、匯出時間
- 各日則數總表（日期 × 主題的表格），一眼看出哪幾天有資料
- 每天一個 Heading 1，每個主題 Heading 2，帶上該日「今日重點」導讀
- 每則新聞：標題做成**可點的超連結**、官方／媒體標記、來源、時間、摘要、重點條列

兩個實作上的坑：

- **中文字型**：python-docx 只設 `font.name` 對中文無效，Word 會用預設西文字型硬撐，
  字距很醜。必須另外把 `w:rFonts` 的 `w:eastAsia` 設成中文字型（這裡用微軟正黑體），
  Normal / Title / Heading 各個 style 與每個 run 都要設。
- **超連結**：python-docx 沒有 API，得自己 `part.relate_to(url, RT.HYPERLINK,
  is_external=True)` 建 relationship，再手工組 `w:hyperlink` 節點。
  同一份文件裡重複的網址 python-docx 會自動共用同一個 relationship，不會膨脹。

可選項目：只收錄官方來源、是否含摘要／重點條列／總表、每主題每天最多幾則、
日期由新到舊或由舊到新。另外附一顆「打包區間 JSON（zip）」給要做後續處理的情況。

產生 Word 放在按鈕後面、bytes 存進 session_state 再交給 `st.download_button`，
避免每次 rerun 都重新排版一次（90 天的文件約需數秒）。

---

### 階段十二 — 使用者設定撐過重新部署

原本的作法是提示使用者手動下載 `topics.json` 覆蓋回 repo —— 實際用起來很容易忘記，
而且**每天排程 commit 新聞後 Streamlit Cloud 就會自動重新部署一次**，
等於當天新增的主題與類別隔天就不見了。改成用 GitHub Contents API 直接回寫
（`core/github_sync.py`）：`store.save_topics()` 與 `source_registry.save_custom()`
存完本機檔案後順手 commit 回 repo，重新部署時容器拉到的就是使用者的最新設定。

Token 來源沿用 `llm.py` 的三段式優先序（當場輸入 → 環境變數 → Streamlit Secrets），
需要 fine-grained token 且 Contents 為 Read and write。

幾個必須處理的細節：

- **不能在 GitHub Actions 裡回寫**：排程本來就會自己 commit，若 runner 環境剛好
  有 `GITHUB_TOKEN` 就會變成排程一邊 commit、一邊又打 API 寫同一個檔案。
  以 `GITHUB_ACTIONS` 環境變數判斷，在 Actions 中 `enabled()` 一律回傳 False。
- **空 commit 會造成無謂的重新部署**：每個 commit 都會觸發 Streamlit Cloud 重建，
  但 `updated_at` 每次存檔都會變，按一次「儲存」但什麼都沒改也會產生差異。
  比對時把 `updated_at` 拿掉再比，內容實質相同就跳過不寫。
- **sha 衝突**：Contents API 更新檔案要帶當前 sha，若排程剛好同時在改會回 409。
  收到 409 就重新取一次 sha 再試一次。
- **失敗不能擋住存檔**：`autosync()` 不丟例外，本機檔案照樣寫入，
  同步結果存進 session_state 顯示在主題管理頁上，網路不通也只是退回原本的手動流程。

畫面上在主題管理頁加了「🔗 自動保存到 GitHub」：狀態、測試 Token、
手動「立即推回」與「從 GitHub 取回」；原本的下載／上傳 JSON 保留為未設 Token 時的備援。
另外在「歷史資料」加了一顆把當天快照推回 repo 的按鈕 ——
網站上手動按「立即更新」抓到的新聞同樣是暫時的，推回去才會進入歷史紀錄與 Word 匯出。

---

## 技術限制說明

**Streamlit Community Cloud 的檔案系統是暫時性的**

服務重啟後會還原成 GitHub 上的版本，因此：

- 「權威版本」的資料放在 repo 裡，由 GitHub Actions 每天更新並 commit
- Streamlit 端主要負責讀取
- 使用者在網站上新增的主題只在當次容器有效，設定 GitHub Token 後由
  `core/github_sync.py` 自動回寫 repo 才會永久保留（見階段十二）；
  未設 Token 時仍可用主題管理頁的手動下載／上傳 `topics.json`

**Groq 免費層**

摘要一次只送 6 則，避免輸出長度超限。所有 LLM 呼叫失敗時都回傳 `None`
而不是丟例外，呼叫端一律有不依賴 LLM 的備援路徑 —— 沒有金鑰時整個網站仍可正常運作。

---

## 檔案結構

```
news-radar/
├── streamlit_app.py            # 網站入口，側欄頁面依主題類別動態分組產生
├── core/
│   ├── defaults.py             # 內建主題的出廠定義、類別清單與預設收錄天數
│   ├── sources.py              # 內建 RSS 來源目錄（每個都經過實測，含來源類別 kind）
│   ├── source_registry.py      # 內建 + 使用者自訂來源的合併與 CRUD
│   ├── keywords.py             # 領域詞庫展開 + 從實際新聞統計候選關鍵字
│   ├── topics.py               # 主題 CRUD + 自動產生搜尋設定
│   ├── collector.py            # 抓取 → 時間窗 → 關鍵字評分 → 去重 → 來源分散
│   ├── summarizer.py           # Groq 摘要 + 無金鑰時的擷取式備援
│   ├── llm.py                  # Groq 呼叫封裝
│   ├── store.py                # JSON 讀寫
│   ├── pipeline.py             # 抓取＋摘要＋存檔的完整流程
│   ├── report.py               # 區間快照 → Word（.docx）／JSON zip 匯出
│   ├── github_sync.py          # 主題／來源設定自動回寫 GitHub repo
│   └── views.py                # Streamlit 共用畫面元件
├── data/
│   ├── topics.json             # 主題設定（目前狀態）
│   ├── sources.json            # 使用者自訂的 RSS 來源
│   └── news/YYYY-MM-DD.json    # 每日新聞快照（保留最近 60 天）
├── scripts/update_news.py      # 每日更新腳本
├── .github/workflows/daily-update.yml
├── requirements.txt
├── README.md
└── 開發紀錄.md
```

---

## 測試方式

- **頁面渲染**：用 `streamlit.testing.v1.AppTest` 逐頁執行，抓出 runtime 例外
- **抓取管線**：實際跑 `scripts/update_news.py` 後檢視輸出，逐則確認相關性
  （這是唯一能發現「麗寶樂園」那類問題的方法）
- **LLM 串接**：用假的 `chat_json` 回應驗證分批、id 對應、bullets、導讀的資料流，
  不需要真的金鑰
- **主題 CRUD**：分類分組、指定類別新增、改類別、刪內建主題、還原、
  單一主題重設、清空後自動建檔，共七項
- **GitHub 回寫**：用假的 `requests.get/put` 模擬 repo，驗證新建、內容相同時跳過、
  更新、409 後重試、讀回、唯讀 Token 的錯誤訊息；另外驗證未設 Token 與
  `GITHUB_ACTIONS=true` 時完全不連線，存檔照常
- **Word 匯出**：用 `AppTest` 實際按下「產生 Word 檔」，再用 python-docx 把產物讀回來
  驗證段落、Heading 階層、總表數字與超連結 relationship；多日情境用複製快照到
  暫存目錄、覆寫 `store.NEWS_DIR` 的方式測，不動到 repo 內的真實資料
- **自訂來源與天數**：壞網址被擋、真 RSS 驗證、新增並指定主題、重複網址被擋、
  合併清單、實際抓取確認有用到、每主題天數覆寫、關掉搜尋後彙整來源歸零、
  刪除時同步從主題移除，共九項

---

## 待改善項目

- [ ] 設定 `GROQ_API_KEY`（Streamlit secrets 與 GitHub Actions secret 兩邊都要），
      目前摘要仍是原文清理版；設定後按「重新產生今天的摘要」即可換成 AI 摘要
- [ ] LLM 摘要路徑目前只用假回應驗證過資料流，尚未實際呼叫過 Groq
- [x] ~~使用者在網站上新增的主題無法自動存回 repo~~ ——
      階段十二已用 GitHub Contents API 自動回寫，只需在 Streamlit Secrets 設
      `GITHUB_TOKEN`（fine-grained，Contents: Read and write）
- [ ] 尚未用真實 Token 實際跑過一次回寫（目前是以假的 API 回應驗證各分支）
- [ ] 台灣政府機關可用的 feed 只有 4 個，經濟部、國科會等仍缺官方來源，
      可考慮改用 data.gov.tw 的開放資料 API
- [ ] 定期執行來源健康檢查（目前是手動按按鈕），可考慮納入每日排程並在失效時通知
- [ ] 關鍵字目前是靜態設定，可考慮依實際命中率自動調整權重
